import Varants from './varants.vue'
import Colors from './colors.vue'
import Icons from './icons.vue'
import Sizes from './sizes.vue'
import Loading from './loading.vue'
import Disable from './disable.vue'
import Split from './split.vue'

## Button

### 基础用法

使用 `varant` 字段控制按钮的外观。

<Varants />

### 分割按钮

<Split />

### 颜色

使用 `color` 字段控制按钮的颜色。

<Colors />

### 图标

使用 `icon` 字段设置按钮的图标。

Tip: 图标库使用 [@iconify/vue](https://iconify.design/docs/icon-components/vue/)

<Icons />

### 尺寸

使用 `size` 字段控制按钮的尺寸。

<Sizes />

### 加载状态

使用 `loading` 字段控制按钮的加载状态。

<Loading />

### 禁用状态

使用 `disabled` 字段控制按钮的禁用状态。

<Disable />

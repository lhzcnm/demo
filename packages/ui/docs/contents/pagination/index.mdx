import Basic from './basic.vue'
import FixWidth from './fix-width.vue'
import Sizes from './sizes.vue'
import Layouts from './layouts.vue'
import Simple from './simple.vue'

## Pagination

### 基础用法

<Basic />

### 固定宽度

默认情况下分页器都是固定宽度的。

如果你不需要固定宽度，可以设置 `fix-width` 属性为 `false`。

<FixWidth />

### 分页大小

通过 `sizes` 属性可以设置分页器的可选分页大小。

<Sizes />

### 布局

通过 `layouts` 属性可以自定义分页器的布局。

<Layouts />

### 简洁模式

<Simple />
